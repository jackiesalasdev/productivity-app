<div class="new_task_whole">
  <%= form_with(model: @category,class:'new_task_form') do |form| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% @category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="newt_field">
    <%= form.label :name, class:'newt_title'%>
    <%= form.text_field :name, class:'newt_input'%>
  </div>  

  



  <div class="actions">
    <%=form.submit(class:'newt_btn')%>
    <br>
  </div>
  <%= link_to 'Back', tasks_path ,class:"newt_back" %>

<% end %>

</div>
</div>

















