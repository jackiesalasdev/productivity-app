<p id="notice"><%= notice %></p>



<div class='feed-whole'>
<%= link_to 'Add Category', new_category_path ,class:"newt_button"%>


  <div class = "feed-table-container">

    
      <% @categories.each do |category| %>

      <div class=feed-card>
      <div class="card-header"><%= category.name %></div>
      <div class="card-body">


        </div>
    
        <div class="actions1"> 
        <%= link_to 'See more |', category ,class:"acts"%>
        <%= link_to 'Edit |', edit_category_path(category), class:"acts" %>
        <%= link_to 'Delete', category, method: :delete, data: { confirm: 'Are you sure?' }, class:"acts"  %>
      </div>
      </div>

  
      <% end %>

    

    </div>

  </div>

</div>

#----------------------------------------------------------------------


<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td></td>
        <td><%= link_to 'Show', category %></td>
        <td><%= link_to 'Edit', edit_category_path(category) %></td>
        <td><%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Category', new_category_path %>
